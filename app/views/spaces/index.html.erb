<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(https://www.deskmag.com/images/articleimages/2021_coworking_images/1200x530/Uncommon_London_2019.jpg)">
  <div class="container">
    <h1>The world's best coworking spaces at your fingertips</h1>
    <h6>Finding your ideal work-space just became so much easier</h6>
    <button class="btn-ghost"><%=link_to "host your space", new_space_path%></button>
  </div>
</div>


<br>

<div class="container d-flex">
<%# div for search bar and cards%>
  <div class="container mt-4">
    <%= form_with url: spaces_path, method: :get, class: "d-flex" do %>
      <%= text_field_tag :query,
        params[:query],
        class: "form-control",
        placeholder: "Find a coworking space"
      %>
      <%= submit_tag "Search", class: "btn btn-primary" %>
    <% end %>

    <div class="cards d-grid p-2">
      <%  @spaces.each do |space| %>
        <%= render "spaces/card", space: space %>
      <% end %>
    </div>
  </div>

<div class="container mt-4">
    <div class = "fixed-element" style="width: 100%; height: 600px;"
      data-controller="map"
      data-map-markers-value="<%= @markers.to_json %>"
      data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
</div>
</div>
